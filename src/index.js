import React from "react";
import ReactDOM from "react-dom/client";
import "./allStyles.css/index.css";
import App from "./modules.js/App.js";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
